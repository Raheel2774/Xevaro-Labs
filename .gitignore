# dependencies
/node_modules

# next.js
/.next
/out
/build

# production
/dist

# env / secrets
.env
.env*.local
.env.production

# source frame archives (served copies live in /public)
/Frames Zip
/Robot

# misc
.DS_Store
*.log
/tmp

# claude code local
/.claude

# outbound engine: scraped leads, generated demos, send logs, SMTP creds
/outbound/data
/outbound/send/inboxes.json

# social engine: content plan, posts, harvested engagers, leads, queues
/social/data

# outbound engine moved to private repo: Raheel2774/xevaro-outbound
/outbound/
/CO_cleaning_no_website_leads.xlsx
/CO_plumbers_no_website_leads.csv

# internal marketing strategy (Reddit/GEO playbooks) — keep out of public repo
/marketing/
.vercel
.env*
